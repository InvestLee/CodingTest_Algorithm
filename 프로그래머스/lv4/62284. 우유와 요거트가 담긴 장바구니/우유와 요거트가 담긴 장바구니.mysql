-- 코드를 입력하세요
SELECT CART.CART_ID
FROM (SELECT CART_ID, NAME
      FROM CART_PRODUCTS 
      GROUP BY CART_ID, NAME
      HAVING NAME IN ('Milk','Yogurt')
     ) CART
GROUP BY CART.CART_ID
HAVING COUNT(CART.CART_ID) >= 2
ORDER BY CART.CART_ID limit 2
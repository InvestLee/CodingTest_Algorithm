WITH RECURSIVE TEMP_TABLE AS(
    SELECT 0 AS HOUR
    UNION ALL
    SELECT HOUR+1 FROM TEMP_TABLE WHERE HOUR < 23
)

SELECT HOUR, COUNT(ANIMAL_ID) FROM TEMP_TABLE
    LEFT OUTER JOIN ANIMAL_OUTS ON HOUR(DATETIME) = TEMP_TABLE.HOUR
GROUP BY HOUR
ORDER BY HOUR ASC